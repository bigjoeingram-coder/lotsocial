-- I-22: enforce NOT NULL on TEXT PRIMARY KEY columns in an existing database.
--
-- GENERATED by scripts/i22-generate-repair.mjs from SCHEMA_BOOTSTRAP_SQL.
-- Do not edit by hand; regenerate instead.
--
-- These statements are DESTRUCTIVE (DROP TABLE after copy). Take a D1 export
-- before applying. Apply as a single batch so the copy and swap are atomic.
-- Applying this to production requires Joe's approval and is not automated.

CREATE TABLE authorization_requests__i22 (
    id TEXT PRIMARY KEY NOT NULL,
    approval_token_hash TEXT NOT NULL UNIQUE,
    dealership_name TEXT NOT NULL,
    rooftop_location TEXT NOT NULL,
    dealership_domain TEXT NOT NULL DEFAULT '',
    associate_name TEXT NOT NULL,
    associate_email TEXT NOT NULL,
    manager_name TEXT NOT NULL,
    manager_title TEXT NOT NULL,
    manager_email TEXT NOT NULL,
    manager_phone TEXT NOT NULL DEFAULT '',
    provider_name TEXT NOT NULL DEFAULT 'Unknown',
    provider_contact_name TEXT NOT NULL DEFAULT '',
    provider_contact_email TEXT NOT NULL DEFAULT '',
    requested_permissions TEXT NOT NULL,
    approved_permissions TEXT,
    status TEXT NOT NULL DEFAULT 'requested',
    email_delivery_status TEXT NOT NULL DEFAULT 'pending',
    email_message_id TEXT,
    typed_signature TEXT,
    manager_notes TEXT NOT NULL DEFAULT '',
    terms_version TEXT NOT NULL DEFAULT '2026-07-18-v1',
    requested_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    decided_at TEXT,
    expires_at TEXT,
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );

INSERT INTO authorization_requests__i22 (id, approval_token_hash, dealership_name, rooftop_location, dealership_domain, associate_name, associate_email, manager_name, manager_title, manager_email, manager_phone, provider_name, provider_contact_name, provider_contact_email, requested_permissions, approved_permissions, status, email_delivery_status, email_message_id, typed_signature, manager_notes, terms_version, requested_at, decided_at, expires_at, created_at, updated_at) SELECT id, approval_token_hash, dealership_name, rooftop_location, dealership_domain, associate_name, associate_email, manager_name, manager_title, manager_email, manager_phone, provider_name, provider_contact_name, provider_contact_email, requested_permissions, approved_permissions, status, email_delivery_status, email_message_id, typed_signature, manager_notes, terms_version, requested_at, decided_at, expires_at, created_at, updated_at FROM authorization_requests;

DROP TABLE authorization_requests;

ALTER TABLE authorization_requests__i22 RENAME TO authorization_requests;

CREATE INDEX IF NOT EXISTS authorization_requests_status_idx ON authorization_requests(status, requested_at DESC);

CREATE TABLE provider_verifications__i22 (
    request_id TEXT PRIMARY KEY NOT NULL,
    verification_token_hash TEXT NOT NULL UNIQUE,
    provider_name TEXT NOT NULL,
    contact_name TEXT NOT NULL,
    contact_email TEXT NOT NULL,
    delivery_method TEXT NOT NULL DEFAULT '',
    feed_format TEXT NOT NULL DEFAULT '',
    connection_notes TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'pending',
    typed_signature TEXT,
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    decided_at TEXT,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );

INSERT INTO provider_verifications__i22 (request_id, verification_token_hash, provider_name, contact_name, contact_email, delivery_method, feed_format, connection_notes, status, typed_signature, created_at, decided_at, updated_at) SELECT request_id, verification_token_hash, provider_name, contact_name, contact_email, delivery_method, feed_format, connection_notes, status, typed_signature, created_at, decided_at, updated_at FROM provider_verifications;

DROP TABLE provider_verifications;

ALTER TABLE provider_verifications__i22 RENAME TO provider_verifications;

CREATE TABLE imported_vehicles__i22 (
    id TEXT PRIMARY KEY NOT NULL,
    associate_email TEXT NOT NULL,
    source_url TEXT NOT NULL,
    source_host TEXT NOT NULL,
    title TEXT NOT NULL,
    vin TEXT NOT NULL DEFAULT '',
    stock_number TEXT NOT NULL DEFAULT '',
    year TEXT NOT NULL DEFAULT '',
    make TEXT NOT NULL DEFAULT '',
    model TEXT NOT NULL DEFAULT '',
    trim TEXT NOT NULL DEFAULT '',
    price TEXT NOT NULL DEFAULT '',
    currency TEXT NOT NULL DEFAULT 'USD',
    description TEXT NOT NULL DEFAULT '',
    image_urls TEXT NOT NULL DEFAULT '[]',
    facts TEXT NOT NULL DEFAULT '{}',
    source_type TEXT NOT NULL DEFAULT 'vdp_one_time',
    authorization_certified_at TEXT NOT NULL,
    imported_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(associate_email, source_url)
  );

INSERT INTO imported_vehicles__i22 (id, associate_email, source_url, source_host, title, vin, stock_number, year, make, model, trim, price, currency, description, image_urls, facts, source_type, authorization_certified_at, imported_at, updated_at) SELECT id, associate_email, source_url, source_host, title, vin, stock_number, year, make, model, trim, price, currency, description, image_urls, facts, source_type, authorization_certified_at, imported_at, updated_at FROM imported_vehicles;

DROP TABLE imported_vehicles;

ALTER TABLE imported_vehicles__i22 RENAME TO imported_vehicles;

CREATE INDEX IF NOT EXISTS imported_vehicles_associate_idx ON imported_vehicles(associate_email, imported_at DESC);

CREATE TABLE creative_projects__i22 (
    id TEXT PRIMARY KEY NOT NULL,
    vehicle_id TEXT NOT NULL,
    associate_email TEXT NOT NULL,
    selected_images TEXT NOT NULL DEFAULT '[]',
    style TEXT NOT NULL,
    duration_seconds INTEGER NOT NULL DEFAULT 30,
    voiceover_script TEXT NOT NULL,
    social_caption TEXT NOT NULL,
    end_card_name TEXT NOT NULL,
    end_card_phone TEXT NOT NULL DEFAULT '',
    end_card_email TEXT NOT NULL DEFAULT '',
    end_card_cta TEXT NOT NULL DEFAULT 'Message me for details',
    status TEXT NOT NULL DEFAULT 'draft',
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );

INSERT INTO creative_projects__i22 (id, vehicle_id, associate_email, selected_images, style, duration_seconds, voiceover_script, social_caption, end_card_name, end_card_phone, end_card_email, end_card_cta, status, created_at, updated_at) SELECT id, vehicle_id, associate_email, selected_images, style, duration_seconds, voiceover_script, social_caption, end_card_name, end_card_phone, end_card_email, end_card_cta, status, created_at, updated_at FROM creative_projects;

DROP TABLE creative_projects;

ALTER TABLE creative_projects__i22 RENAME TO creative_projects;

CREATE INDEX IF NOT EXISTS creative_projects_associate_idx ON creative_projects(associate_email, created_at DESC);

CREATE INDEX IF NOT EXISTS creative_projects_vehicle_idx ON creative_projects(vehicle_id, created_at DESC);

CREATE TABLE creative_render_jobs__i22 (
    id TEXT PRIMARY KEY NOT NULL,
    project_id TEXT NOT NULL,
    associate_email TEXT NOT NULL,
    provider TEXT NOT NULL DEFAULT 'shotstack',
    provider_render_id TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'prepared',
    render_plan TEXT NOT NULL,
    output_url TEXT NOT NULL DEFAULT '',
    storage_key TEXT NOT NULL DEFAULT '',
    error_message TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );

INSERT INTO creative_render_jobs__i22 (id, project_id, associate_email, provider, provider_render_id, status, render_plan, output_url, storage_key, error_message, created_at, updated_at) SELECT id, project_id, associate_email, provider, provider_render_id, status, render_plan, output_url, storage_key, error_message, created_at, updated_at FROM creative_render_jobs;

DROP TABLE creative_render_jobs;

ALTER TABLE creative_render_jobs__i22 RENAME TO creative_render_jobs;

CREATE INDEX IF NOT EXISTS creative_render_jobs_project_idx ON creative_render_jobs(project_id, created_at DESC);

CREATE INDEX IF NOT EXISTS creative_render_jobs_associate_idx ON creative_render_jobs(associate_email, created_at DESC);
